CREATE TABLE IF NOT EXISTS operation_data(
   Process Id VARCHAR(255),
   Client Id VARCHAR(255),
   Recorded DATE,
   Process Name VARCHAR(255),
   Duration INTEGER,
   Cost INTEGER,
   Number of Errors INTEGER
)
